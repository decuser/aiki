# concat smoke test
let list = import("list")

println(list.concat([1, 2], [3, 4]))
println(list.concat([], [1, 2]))
println(list.concat([1], []))
println(list.concat([], []))
println(length(list.concat([1, 2, 3], [4, 5])))
println("CONCAT_SMOKE_DONE")
