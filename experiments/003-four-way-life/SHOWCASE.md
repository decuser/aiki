# Interactive Showcase

## Interactive showcase

For the full presentation:

    cd experiment
    ./showcase.sh

This opens four worker-status terminals while the current terminal remains the
coordinator/log terminal and the coordinator owns the canvas. The worker views
display activity from the actual four coordinator-owned worker processes without
changing the validated process-pipe protocol.
