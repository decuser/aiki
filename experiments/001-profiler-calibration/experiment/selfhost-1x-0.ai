let bootstrap = import("selfhost/bootstrap")
let result = 9
let i = 0

while i < 0 {
    result = bootstrap.run("1 + 2 * 3\n", "probe.ai")
    i = i + 1
}

if not equal(result, 9) {
    println("FAIL: expected 9, got " + inspect(result))
}
