# 1. Define your atomic tools
let square = (x) {
    return x * x
}

let is_even = (x) {
    return x % 2 == 0
}

# 2. The Pipeline
# Reads left-to-right: Generate -> Filter -> Transform -> Aggregate
range(1, 11)
|> filter(is_even)
|> map(square)
|> sum()
|> print()

print("\n")
