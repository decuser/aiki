let list = import("list")

# down.ai
# Non tail recursion should hit Aiki stack_limit and return stack overflow error.
stack_limit(10000)

let down = (n) {
	if (n <= 0) {
		return 0
	}
	return (1 + down((n - 1)))
}

list.println(down(200000))
