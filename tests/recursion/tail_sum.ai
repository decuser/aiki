# tail_sum.ai
# Tail recursion should not grow Go stack with proper tail calls.
stack_limit(10000)

let sum_tail = (n, acc) {
	if (n <= 0) {
		return acc
	}
	return sum_tail((n - 1), (acc + n))
}

println(sum_tail(20000000, 0))
