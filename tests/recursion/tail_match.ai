# tail_match.ai
# Tail calls in match arms without explicit return should be optimized.
stack_limit(10000)

let sum_match = (n, acc) {
	match n {
		0 {
			acc
		}
		_ {
			sum_match((n - 1), (acc + n))
		}
	}
}

println(sum_match(20000000, 0))
