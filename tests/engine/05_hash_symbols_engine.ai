# 05_hash_symbols_engine.ai
# Hash, symbol keys, string keys, numbers

let h = hash_new()
let h = hash_put(h, "name", "Mochi")
let h = hash_put(h, :age, 3)
let h = hash_put(h, 1, "one")

print(hash_get(h, "name"), "\n")
print(hash_get(h, :age), "\n")
print(hash_get(h, 1), "\n")
